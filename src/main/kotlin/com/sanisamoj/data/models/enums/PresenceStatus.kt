package com.sanisamoj.data.models.enums

enum class PresenceStatus {
    MARKED_PRESENT, // The user marked their attendance
    ATTENDED,       // The user actually attended the event
    DID_NOT_ATTEND  // The user did not attend the event
}